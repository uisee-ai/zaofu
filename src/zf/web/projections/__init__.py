"""Read-side projections extracted from web/server.py (P1 seam 1).

Each module is a layer in an acyclic import order; server.py re-exports
every moved symbol so call sites and tests are unchanged.
"""
