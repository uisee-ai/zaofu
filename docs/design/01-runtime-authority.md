# Runtime Authority

ZaoFu keeps deterministic runtime authority in the Kernel. Role agents report
semantic intent through artifacts, events, or controlled actions; they do not
own canonical state transitions directly.

Use `142-layered-runtime-authority-and-orchestration-modes.md` for the current
route family and operational mode details.
